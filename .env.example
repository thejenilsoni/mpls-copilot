# Runtime mode is intentionally local by default.
NEXT_PUBLIC_RUNTIME_MODE=air-gapped

# Optional local inference endpoint. Never point this at a public service
# when operating in an isolated environment.
LOCAL_MODEL_ENDPOINT=http://127.0.0.1:11434
LOCAL_MODEL_NAME=qwen3:4b
